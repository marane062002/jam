import { ValiderDemandeMissionDTO } from './valider-demande-mission-dto';

describe('ValiderDemandeMissionDTO', () => {
  it('should create an instance', () => {
    expect(new ValiderDemandeMissionDTO()).toBeTruthy();
  });
});
