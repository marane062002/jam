import { QuantiteReapprovisionnement } from './quantite-reapprovisionnement';

describe('QuantiteReapprovisionnement', () => {
  it('should create an instance', () => {
    expect(new QuantiteReapprovisionnement()).toBeTruthy();
  });
});
