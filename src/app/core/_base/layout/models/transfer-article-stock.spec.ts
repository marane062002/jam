import { TransferArticleStock } from './transfer-article-stock';

describe('TransferArticleStock', () => {
  it('should create an instance', () => {
    expect(new TransferArticleStock()).toBeTruthy();
  });
});
