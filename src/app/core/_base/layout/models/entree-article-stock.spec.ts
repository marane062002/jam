import { EntreeArticleStock } from './entree-article-stock';

describe('EntreeArticleStock', () => {
  it('should create an instance', () => {
    expect(new EntreeArticleStock()).toBeTruthy();
  });
});
