import { ConsultationArchitecturale } from './consultation-architecturale';

describe('ConsultationArchitecturale', () => {
  it('should create an instance', () => {
    expect(new ConsultationArchitecturale()).toBeTruthy();
  });
});
