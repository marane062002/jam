import { PersonneExterneCA } from './personne-externe-c-a';

describe('PersonneExterneCA', () => {
  it('should create an instance', () => {
    expect(new PersonneExterneCA()).toBeTruthy();
  });
});
